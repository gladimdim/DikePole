name: server
description: A simple server to upload and publish markdown version of stories


environment:
  sdk: '>=2.6.0 <3.0.0'

dependencies:
  nanoid: ^0.1.0
  gladstoriesengine: ^0.3.1
  markdown: ^3.0.0

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
  angel_framework: ^2.0.0
